apiVersion: v2
name: tssc-app-namespaces
description: Red Hat User Application Namespaces
type: application
version: "1.7.0"
annotations:
  tssc.redhat-appstudio.github.com/depends-on: tssc-acs, tssc-pipelines, tssc-tpa
  tssc.redhat-appstudio.github.com/integrations-required: "(bitbucket || github || gitlab) && acs && trustification"
